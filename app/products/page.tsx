import type {Metadata} from 'next';import Link from 'next/link';import {ArrowRight} from 'lucide-react';import {CATEGORY_LINKS} from '@/lib/catalog-config';
export const metadata:Metadata={title:{absolute:'Construction Machinery Products | ANLAN Machinery'},description:'Browse new excavators, loaders, rollers, motor graders and self loading concrete mixers by category.',alternates:{canonical:'/products'}};
export default function Products(){return <section className="section pt-36"><div className="wrap"><p className="eyebrow">Product categories</p><h1 className="title mt-3">Choose your machine.</h1><p className="sub mt-5">Open a dedicated category to compare the right models without mixing unrelated equipment.</p><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{CATEGORY_LINKS.map((item,index)=><Link href={`/products/${item.path}`} key={item.key} className="group flex min-h-44 items-end justify-between rounded-3xl bg-ink p-7 text-white transition hover:bg-lime hover:text-ink"><span><small className="text-white/40 group-hover:text-black/40">0{index+1}</small><h2 className="mt-2 text-3xl font-black">{item.label}</h2></span><ArrowRight/></Link>)}</div></div></section>}
