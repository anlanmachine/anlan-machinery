import Link from 'next/link';import {CATEGORY_LINKS,type CatalogCategory} from '@/lib/catalog-config';
export function CategoryNavigation({active}:{active?:CatalogCategory}){return <nav aria-label="Product categories" className="-mx-5 mt-9 overflow-x-auto px-5"><div className="flex min-w-max gap-2">{CATEGORY_LINKS.map(item=><Link key={item.key} href={`/products/${item.path}`} aria-current={active===item.key?'page':undefined} className={`rounded-full px-5 py-3 text-sm font-black transition ${active===item.key?'bg-lime text-ink shadow-sm':'border border-black/15 bg-white text-gray-600 hover:border-black hover:text-ink'}`}>{item.label}</Link>)}</div></nav>}
