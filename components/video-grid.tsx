import type {MediaVideo} from '@/lib/media';
export function VideoGrid({videos,limit}:{videos:MediaVideo[];limit?:number}){const visible=videos.slice(0,limit||videos.length);if(!visible.length)return <div className="rounded-3xl border border-dashed border-black/20 p-12 text-center"><b>Waiting For Material</b><p className="mt-2 text-gray-500">No matching video has been supplied yet.</p></div>;return <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{visible.map(video=><article className="overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-black/5" key={video.id}><video controls playsInline preload="metadata" poster={video.poster} className="aspect-[4/3] w-full bg-black object-cover"><source src={video.src} type="video/mp4"/>Your browser does not support video playback.</video><div className="p-5"><h3 className="font-black">{video.title}</h3><p className="mt-1 text-sm text-gray-500">{Math.max(1,Math.round(video.duration))} sec · optimized MP4</p></div></article>)}</div>}
