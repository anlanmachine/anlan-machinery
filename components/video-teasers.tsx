import Image from 'next/image';import Link from 'next/link';import {Play} from 'lucide-react';import type {MediaVideo} from '@/lib/media';
export function VideoTeasers({videos}:{videos:MediaVideo[]}){return <div className="grid gap-6 md:grid-cols-3">{videos.slice(0,3).map(video=><Link href="/media" className="group overflow-hidden rounded-3xl bg-white shadow-sm" key={video.id}><div className="relative aspect-[4/3] bg-black"><Image src={video.poster} alt={video.alt} fill loading="lazy" className="object-cover" sizes="(max-width:768px) 100vw,33vw"/><span className="absolute inset-0 grid place-items-center bg-black/15"><span className="grid size-14 place-items-center rounded-full bg-lime text-ink transition group-hover:scale-110"><Play fill="currentColor"/></span></span></div><div className="p-5"><h3 className="font-black">{video.title}</h3><p className="mt-1 text-sm text-gray-600">Watch in Media Center</p></div></Link>)}</div>}
