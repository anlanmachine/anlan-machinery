'use client';
import {useMemo,useState} from 'react';import Link from 'next/link';import {ArrowRight,Search} from 'lucide-react';import type {SeoPost} from '@/lib/blog';
export function BlogIndex({posts}:{posts:SeoPost[]}){const [category,setCategory]=useState('All'),[query,setQuery]=useState(''),categories=['All',...new Set(posts.map(post=>post.category))],visible=useMemo(()=>posts.filter(post=>(category==='All'||post.category===category)&&`${post.title} ${post.model}`.toLowerCase().includes(query.toLowerCase())),[posts,category,query]);return <><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div className="flex flex-wrap gap-2">{categories.map(item=><button onClick={()=>setCategory(item)} className={`rounded-full px-4 py-2 text-sm font-bold ${category===item?'bg-lime':'border border-black/15'}`} key={item}>{item}</button>)}</div><label className="relative min-w-72"><Search className="absolute left-3 top-3 text-gray-400" size={18}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search model or topic" className="w-full rounded-full border border-black/15 py-2.5 pl-10 pr-4"/></label></div><p className="mt-7 text-sm text-gray-500">Showing {visible.length} articles</p><div className="mt-7 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{visible.map(post=><Link href={`/blog/${post.slug}`} key={post.slug} className="group rounded-3xl border border-black/10 p-7 transition hover:-translate-y-1 hover:bg-white hover:shadow-xl"><p className="text-xs font-black uppercase tracking-wider text-[#789400]">{post.category} · {post.date}</p><h2 className="mt-4 text-xl font-black leading-snug">{post.title}</h2><p className="mt-4 line-clamp-3 text-sm leading-6 text-gray-500">{post.excerpt}</p><span className="mt-6 flex items-center gap-2 font-black">Read article <ArrowRight size={17} className="transition group-hover:translate-x-1"/></span></Link>)}</div></>}
